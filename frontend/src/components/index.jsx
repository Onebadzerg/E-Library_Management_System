import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Import CSS styles (you can modify this based on your needs)
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
